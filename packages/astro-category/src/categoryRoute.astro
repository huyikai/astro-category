---
import CategoryItem from './categoryItem.astro';

interface Props {
  category: any;
  dir?: string;
  linkStyle?: string;
}

const { category, dir = 'posts', linkStyle } = Astro.props;
---

<p>Current Category: {category.dir}</p>

{
  category.subCategory && category.subCategory.length > 0 && (
    <>
      <h2>SubCategory</h2>
      {category.subCategory.map((item: any) => (
        <CategoryItem
          title={item.name}
          url={`/category/${item.path}`}
          style={linkStyle}
        />
      ))}
    </>
  )
}
{
  category.subContent && category.subContent.length > 0 && (
    <>
      <h2>SubContent</h2>
      {category.subContent.map((item: any) => (
        <CategoryItem
          title={item.data.title}
          url={`/${dir}/${item.slug}/`}
          style={linkStyle}
        />
      ))}
    </>
  )
}
