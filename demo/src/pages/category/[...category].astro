---
import { CategoryRoute, categoryHelper } from 'astro-category';

export const getStaticPaths = categoryHelper({ dir: 'posts' });

const { category } = Astro.props;
---

<CategoryRoute
  category={category}
  dir='posts'
/>
